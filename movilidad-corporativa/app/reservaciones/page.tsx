import { PlaceholderPage } from "@/components/placeholder-page";

export default function ReservacionesPage() {
  return (
    <PlaceholderPage
      title="Mis reservaciones"
      description="Esta vista mostrará el estado de cada reserva, su vehículo asignado y la trazabilidad del recorrido."
      highlights={["Reservas activas", "Historial de viajes", "Check-in y check-out"]}
    />
  );
}
